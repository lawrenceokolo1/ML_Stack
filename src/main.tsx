import React from "react";
import ReactDOM from "react-dom/client";
import ProjectsSite from "../portfolio_site";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ProjectsSite />
  </React.StrictMode>
);
